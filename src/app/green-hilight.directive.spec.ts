import { GreenHilightDirective } from './green-hilight.directive';

describe('GreenHilightDirective', () => {
  it('should create an instance', () => {
    const directive = new GreenHilightDirective();
    expect(directive).toBeTruthy();
  });
});
