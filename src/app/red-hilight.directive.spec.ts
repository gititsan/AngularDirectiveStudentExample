import { RedHilightDirective } from './red-hilight.directive';

describe('RedHilightDirective', () => {
  it('should create an instance', () => {
    const directive = new RedHilightDirective();
    expect(directive).toBeTruthy();
  });
});
